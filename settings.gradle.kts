plugins {
    // Auto-provisions the JDK requested by the IntelliJ Platform plugin's toolchain.
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "delivery-pipeline"
